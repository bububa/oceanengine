name: Go

on:
  push:
    branches: [ master, main ]
  pull_request:
    branches: [ master, main ]

jobs:

  build:
    runs-on: ubuntu-latest
    env:
      GOPATH: ${{ github.workspace }}
      package: github.com/bububa/oceanengine
    steps:
    - uses: actions/checkout@v3
      with:
        path: src/${{ env.package }}

    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version-file: ${{ env.GOPATH }}/src/${{ env.package }}/go.mod

    - name: Dependencies
      run: |
        cd ${{ env.GOPATH }}/src/${{ env.package }}
        go mod tidy

    - name: Build
      run: go build -v ${{ env.package }}/...

    - name: Test
      env:
        APPKEY: ${{ secrets.APPKEY }}
        SECRET: ${{ secrets.APP_SECRET }}
      run: go test -v ${{ env.package }}/...
